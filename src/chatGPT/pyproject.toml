[tool.black]
line-length = 88
target-version = ["py311"]

[tool.ruff]
# Configuraciones generales de Ruff
line-length = 88
target-version = "py311"

[tool.ruff.lint]
# Configuraciones específicas del Linter (las que daban el aviso)
select = ["E", "F", "I", "B", "UP"]
ignore = []